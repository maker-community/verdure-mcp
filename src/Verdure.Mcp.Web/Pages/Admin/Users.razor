@page "/admin/users"
@attribute [Authorize(Policy = "AdminPolicy")]
@inject ISnackbar Snackbar
@inject IStringLocalizer<SharedResources> L

<PageTitle>@L["AdminUsers_PageTitle"]</PageTitle>

<MudText Typo="Typo.h4" Class="mb-4">@L["AdminUsers_Title"]</MudText>

<MudText Typo="Typo.body1" Class="mb-4" Color="Color.Secondary">
    @L["AdminUsers_Subtitle"]
</MudText>

<MudPaper Class="pa-8 text-center" Elevation="0">
    <MudIcon Icon="@Icons.Material.Filled.Engineering" Size="Size.Large" Color="Color.Secondary" />
    <MudText Typo="Typo.h6" Class="mt-4">@L["AdminUsers_InDevelopment"]</MudText>
    <MudText Typo="Typo.body2" Color="Color.Secondary">
        @L["AdminUsers_ComingSoon"]
    </MudText>
    <MudText Typo="Typo.caption" Color="Color.Tertiary" Class="mt-4">
        @L["AdminUsers_KeycloakHint"]
    </MudText>
</MudPaper>

@code {
    protected override void OnInitialized()
    {
        Snackbar.Configuration.PositionClass = Defaults.Classes.Position.BottomRight;
    }
}
