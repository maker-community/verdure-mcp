@using Microsoft.AspNetCore.Components.Authorization
@inject IStringLocalizer<SharedResources> L

<CascadingAuthenticationState>
    <Router AppAssembly="@typeof(App).Assembly">
        <Found Context="routeData">
            <AuthorizeRouteView RouteData="@routeData" DefaultLayout="@typeof(MainLayout)">
                <NotAuthorized>
                    <MudContainer Class="mt-16">
                        <MudText Typo="Typo.h5" Class="mb-4">@L["Auth_LoginRequired"]</MudText>
                        <MudText>@L["Auth_LoginRequiredMessage"]</MudText>
                        <MudButton Variant="Variant.Filled" Color="Color.Primary" Href="authentication/login" Class="mt-4">
                            @L["Login"]
                        </MudButton>
                    </MudContainer>
                </NotAuthorized>
                <Authorizing>
                    <MudProgressCircular Color="Color.Primary" Indeterminate="true" />
                </Authorizing>
            </AuthorizeRouteView>
            <FocusOnNavigate RouteData="@routeData" Selector="h1" />
        </Found>
        <NotFound>
            <PageTitle>@L["NotFound_PageTitle"]</PageTitle>
            <LayoutView Layout="@typeof(MainLayout)">
                <MudContainer Class="mt-16 text-center">
                    <MudText Typo="Typo.h4" Class="mb-4">@L["NotFound_Title"]</MudText>
                    <MudText>@L["NotFound_Message"]</MudText>
                    <MudButton Variant="Variant.Filled" Color="Color.Primary" Href="/" Class="mt-4">
                        @L["Home"]
                    </MudButton>
                </MudContainer>
            </LayoutView>
        </NotFound>
    </Router>
</CascadingAuthenticationState>

<MudThemeProvider Theme="_theme" IsDarkMode="@_isDarkMode" />
<MudPopoverProvider />
<MudDialogProvider />
<MudSnackbarProvider />

@code {
    private bool _isDarkMode = false;
    
    private MudTheme _theme = new()
    {
        PaletteLight = new PaletteLight()
        {
            Primary = "#7C4DFF",           // 明亮的浅紫罗兰色
            Secondary = "#5E35B1",         // 深紫色作为辅助色
            Tertiary = "#E91E63",          // 粉红色作为点缀
            Background = "#F5F5F9",        // 极浅的紫灰色背景
            Surface = "#FFFFFF",           // 纯白表面
            AppbarBackground = "#7C4DFF",  // 与主色一致的 AppBar
            AppbarText = "#FFFFFF",
            DrawerBackground = "#FAFAFA",  // 浅灰色抽屉
            DrawerText = "#1C1B1F",
            TextPrimary = "#1C1B1F",
            TextSecondary = "#49454F"
        },
        PaletteDark = new PaletteDark()
        {
            Primary = "#D0BCFF",
            Secondary = "#CCC2DC",
            Tertiary = "#EFB8C8",
            Background = "#1C1B1F",
            Surface = "#1C1B1F",
            AppbarBackground = "#1C1B1F",
            AppbarText = "#E6E1E5",
            DrawerBackground = "#1C1B1F",
            DrawerText = "#E6E1E5",
            TextPrimary = "#E6E1E5",
            TextSecondary = "#CAC4D0"
        },
        Typography = new Typography()
        {
            Default = new DefaultTypography()
            {
                FontFamily = ["Roboto", "Helvetica", "Arial", "sans-serif"]
            },
            H1 = new H1Typography()
            {
                FontWeight = "400",
                FontSize = "3rem"
            },
            H2 = new H2Typography()
            {
                FontWeight = "400",
                FontSize = "2.5rem"
            },
            H3 = new H3Typography()
            {
                FontWeight = "400",
                FontSize = "2rem"
            }
        },
        LayoutProperties = new LayoutProperties()
        {
            DefaultBorderRadius = "16px"
        }
    };
}
